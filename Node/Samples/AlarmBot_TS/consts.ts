
export var entities = {
    alarm_state: 'builtin.alarm.alarm_state',
    duration: 'builtin.alarm.duration',
    start_date: 'builtin.alarm.start_date',
    start_time: 'builtin.alarm.start_time',
    title: 'builtin.alarm.title'
};

export var user = {
    properties: {
        timezone: 'timezone'
    }
}
